# Build artifacts
web-ext-artifacts/
*.xpi

# Claude Code (harness metadata, memory, config)
.claude/
.claude*
CLAUDE.md
.mcp.json

# Reference material — Expression Search NG (not part of this project)
.base-expression_search_ng/
*expression*search*
*expression_search*
expression-search*

# Listing artwork — 1024px raster for the ATN store page, not shipped in the
# .xpi (the add-on itself loads icons/icon.svg). Also excluded in build.sh.
icons/icon.png

# OS / editor cruft
.DS_Store
Thumbs.db
*~
*.bak

# Node (only used for tests; none committed)
node_modules/
